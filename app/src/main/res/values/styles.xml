<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="FullscreenTheme" parent="AppTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
        <item name="metaButtonBarStyle">?android:attr/buttonBarStyle</item>
        <item name="metaButtonBarButtonStyle">?android:attr/buttonBarButtonStyle</item>
    </style>

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

    <style name="CurrentActionBarStyle">
        <item name="background">@color/colorPrimary</item>
        <item name="logo">@drawable/activity_icon</item>
        <item name="displayOptions">useLogo|showHome</item>
        <item name="height">60dp</item>
        <item name="android:layout_gravity">left</item>
    </style>

    <style name="CurrentScreenTheme" parent="AppTheme">
        <item name="actionBarStyle">@style/CurrentActionBarStyle</item>
    </style>
</resources>
